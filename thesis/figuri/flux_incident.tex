\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  node distance=10mm and 8mm,
  flowstep/.style={
    draw,
    rounded corners=3pt,
    align=center,
    minimum height=10mm,
    text width=2.8cm,
    font=\small
  },
  status/.style={
    draw,
    rounded corners=3pt,
    align=center,
    minimum height=9mm,
    text width=2.55cm,
    font=\small
  },
  decision/.style={
    draw,
    diamond,
    aspect=1.9,
    align=center,
    inner sep=1pt,
    text width=2.2cm,
    font=\small
  },
  arrow/.style={-{Stealth[length=2.4mm]}, thick, shorten >=1pt}
]

\node[flowstep] (video) {Cadre video};
\node[flowstep, right=of video] (ml) {Detecție\\și clasificare};
\node[flowstep, right=of ml] (temporal) {Analiză\\temporală};
\node[decision, right=of temporal] (candidate) {Incident\\confirmat?};
\node[status, right=of candidate] (pending) {\texttt{pending}\\incident salvat};

\node[status, above right=9mm and 9mm of pending] (reviewed) {\texttt{reviewed}\\confirmat};
\node[status, right=of pending] (forwarded) {\texttt{forwarded}\\arhivat local};
\node[status, below right=9mm and 9mm of pending] (dismissed) {\texttt{dismissed}\\fals pozitiv};
\node[flowstep, right=16mm of forwarded] (delete) {Ștergere\\sau retenție};

\draw[arrow] (video) -- (ml);
\draw[arrow] (ml) -- (temporal);
\draw[arrow] (temporal) -- (candidate);
\draw[arrow] (candidate) -- node[above, font=\scriptsize]{da} (pending);
\draw[arrow] (candidate.south) -- ++(0,-8mm) -| node[pos=0.25, below, font=\scriptsize]{nu} (temporal.south);
\draw[arrow] (pending) -- (reviewed);
\draw[arrow] (pending) -- (forwarded);
\draw[arrow] (pending) -- (dismissed);
\draw[arrow] (reviewed.east) -- ++(8mm,0) |- ($(delete.west)+(0,3.4mm)$);
\draw[arrow] (forwarded.east) -- (delete.west);
\draw[arrow] (dismissed.east) -- ++(8mm,0) |- ($(delete.west)+(0,-3.4mm)$);

\end{tikzpicture}%
}
\caption{Fluxul de creare și validare a unui incident}
\label{fig:flux-incident}
\end{figure}
